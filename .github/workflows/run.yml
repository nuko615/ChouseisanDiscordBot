name: Run bot

on:
  workflow_dispatch:
    inputs:
      config_path:
        description: TOML configuration file
        required: true
        default: config.toml
        type: string
      dry_run:
        description: Validate without creating an event or posting to Discord
        required: true
        default: true
        type: boolean

permissions:
  contents: read

concurrency:
  group: chouseisan-discord-bot
  cancel-in-progress: false

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -e .
      - name: Dry run
        if: ${{ inputs.dry_run == true }}
        env:
          CONFIG_PATH: ${{ inputs.config_path }}
          DISCORD_ROLE_ID: ${{ vars.DISCORD_ROLE_ID }}
        run: chouseisan-discord-bot --config "$CONFIG_PATH" --dry-run
      - name: Create event and notify Discord
        if: ${{ inputs.dry_run != true }}
        env:
          CONFIG_PATH: ${{ inputs.config_path }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_ROLE_ID: ${{ vars.DISCORD_ROLE_ID }}
        run: chouseisan-discord-bot --config "$CONFIG_PATH"
